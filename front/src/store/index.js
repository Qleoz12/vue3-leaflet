import { Point } from 'leaflet'
import { createStore } from 'vuex'

export default createStore({
  state: {
    onMoveLatitude: Point,
    onMoveLongitude: Point,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
  getters: {
    
  }
})
